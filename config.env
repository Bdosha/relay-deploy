# config.env — VPN deployment settings
# No secrets here. Safe to commit.

# Number of VPN users to generate
NUM_USERS=11

# Xray listen port on exit server
XRAY_PORT=8443

# Reality SNI — must support TLS 1.3
SNI="www.github.com"

# TLS fingerprint
FINGERPRINT="chrome"

# Relay ports that forward to exit node
RELAY_PORTS=(443 8444)

# Cloudflare WARP — route AI/blocked services through Cloudflare instead of direct exit
WARP_ENABLED=true
WARP_PORT=40000

# Domains routed through WARP (AI services that block datacenter IPs)
WARP_DOMAINS=(
    "domain:gemini.google.com"
    "domain:bard.google.com"
    "domain:aistudio.google.com"
    "domain:generativelanguage.googleapis.com"
    "domain:alkalimakersuite-pa.clients6.google.com"
    "domain:makersuite.google.com"
    "domain:deepmind.google"
    "domain:notebooklm.google.com"
    "domain:ai.google.dev"
    "domain:labs.google"
    "domain:openai.com"
    "domain:chatgpt.com"
    "domain:chat.openai.com"
    "domain:claude.ai"
    "domain:anthropic.com"
    "domain:meta.ai"
    "domain:copilot.microsoft.com"
    "domain:perplexity.ai"
    "domain:sora.com"
)
